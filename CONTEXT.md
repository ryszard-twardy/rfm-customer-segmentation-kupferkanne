# Context: project-1-rfm-customer-segmentation

The vocabulary of this domain. When the code, docs, or this team's writing names a concept that lives here, it uses these terms — never synonyms.

## How to use this file

- One-line definitions. No implementation details. No code snippets. No reasoning.
- When a term is resolved during a session (e.g., `/mp-grill-with-docs`), it gets added here on the spot, not batched.
- If a term has a synonym that this repo explicitly avoids, list it in the entry: "(NOT: <synonym>)".
- For multi-context repos (rare), add `CONTEXT-MAP.md` at root and put per-context CONTEXT.md files inside each subsystem.

## Terms

### Core domain

- (none yet — populate via `/mp-grill-with-docs`)

### Boundaries

- (none yet)

### Open questions

- [?] (none yet)
